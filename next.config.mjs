/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuração para Vercel (modo padrão com API routes)
  // Não use 'output: export' pois as API routes não funcionarão
};

export default nextConfig;
